services:
  db:
    image: postgres:alpine
    env_file: 
      - .env.db
    container_name: microservice-users-db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
